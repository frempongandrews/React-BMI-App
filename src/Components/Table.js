import React from 'react';

const Table = React.createClass({
    render () {
        return (
            <div>
                this is the table
            </div>
        )
    }
});

export default Table;